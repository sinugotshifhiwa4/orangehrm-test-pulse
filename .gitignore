# dependencies / build output
node_modules
dist
*.local

# wrangler files (Cloudflare Workers local state / secrets)
.wrangler
.dev.vars*
!.dev.vars.example

# environment
.env*
!.env.example
.playwright-mcp
graphify-out